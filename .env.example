# Vercel 프로젝트 설정 > Settings > Environment Variables 에 아래 두 값을 등록하세요.
# (이 파일 자체는 예시일 뿐이며, 실제 값은 절대 git에 커밋하지 마세요.)

# 네이버 클라우드 플랫폼(NCP) > CLOVA OCR > 도메인 생성 후 발급되는 APIGW Invoke URL
CLOVA_OCR_INVOKE_URL=

# 같은 도메인의 Secret Key
CLOVA_OCR_SECRET_KEY=
